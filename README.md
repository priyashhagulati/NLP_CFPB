# NLP_CFPB
An NLP pipeline classifying CFPB financial complaints into 7 product categories using two approaches: a keyword-based rule classifier (41.8% accuracy) and a TF-IDF + Logistic Regression model (78.5% accuracy), applied to 300K real consumer complaints with text preprocessing via NLTK and scikit-learn.
